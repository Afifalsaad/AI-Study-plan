[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Increase function timeout for PDF processing (up to 15 minutes)
[functions]
  timeout = 900

[[plugins]]
  package = "@netlify/plugin-nextjs"
